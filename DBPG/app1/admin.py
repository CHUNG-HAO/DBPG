from django.contrib import admin

from .models import (
    Suppliers, Brands, Dealers, Customers, Factories, Models,
    Cars, Options, FactoryBrands, DealerBrands, Sales
    )

# Register your models here.
